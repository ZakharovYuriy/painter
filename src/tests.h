#pragma once

#include <iostream>
#include <vector>

#include "graphics.h"

namespace tests {

    void GeneralGraphicsTest (std::ostream& out);

    void ParsingRectanglesFromFile(std::ostream& out);

}  // namespace graph
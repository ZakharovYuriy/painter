#pragma once

#include <iostream>
#include <vector>

#include "graphics.h"

namespace tests {

    void GeneralGraphicsTest (std::ostream& out);

    void ParsingRectangles(std::ostream& out);

}  // namespace graph